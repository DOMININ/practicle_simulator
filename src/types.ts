export type Practicle = {
    id: number;
    x: number; // Позиция по X
    y: number; // Позиция по Y
    vx: number; // Скорость по X
    vy: number; // Скорость по Y
    mass: number; // Масса
    charge: number; // Заряд
};